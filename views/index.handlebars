<style>
    section {
        max-width: 100%;
        margin: 0 auto;
        text-align: center;
    }
</style>
<div id='principal' style="background-color: rgb(40,55,89);">

    <img src="/header/header.svg" style="width: 100%;">
    <table style="background-color: white;width: 100%;height: 50%;">
        <tr>
            <td>
                <img src="/body/texto.svg" class="d-block w-100" style="margin-left: 10%;" data-anime='left' alt="...">
            </td>
            <td>
                <img src="/body/pc.svg" class="d-block" data-anime='right' style="height: 300px;margin-left: 20%;">
            </td>
        </tr>
    </table>


    <!--
    <div class="div" style="width: 100%;">
        <div id="carouselBanner" class="slide" data-bs-ride="carousel">
            <div class="carousel-inner">
                <div class="carousel-item active">
                    <img src="/body/2.svg" class="d-block w-100" alt="...">
                </div>
                <div class="carousel-item">
                    <img src="/body/3.svg" class="d-block w-100" alt="...">
                </div>
            </div>
        </div>
    </div>
    -->

    <div class='fadeIn' style="width: 100%;">
        <img src='/body/passo-a-passo.svg' style="width: 100%;" />
    </div>

    <div style="width: 100%;" data-anime='left' class="fadeIn">
        <form method="post" action='/usuario/enviar'>
            <div class='formulario'>
                <div
                    style="margin-left: 22%;width: fit-content; height: fit;text-align: center;background: rgb(40,55,89);padding: 10px;border-radius: 5px;color: white;">
                    <h6>Faça o Teste Grátis do Sistema de
                        Gestão de Custos de Projetos de Instalação de
                        Usinas Solares</h6>
                </div>
                <div style="margin-top: 20px;margin-left: 35%;">
                    <h6>Cadastre seu e-mail para receber a senha de acesso</h6>
                    <table>
                        <tr>
                            <td><label class="col-form-label col-form-label-sm mb-1">Nome:</label><br></td>
                            <td><input type="text" name='nome' style="width: 320px;"
                                    class="form-control form-control-sm mb-1">
                            </td>
                        </tr>
                        <tr>
                            <td><label class="col-form-label col-form-label-sm mb-1">Celular:</label><br>
                            </td>
                            <td><input type="text" class="form-control form-control-sm mb-1" name='celular' id='celular'
                                    placeholder="(XX) X XXXX-XXXX" maxlength="16" autocomplete="off"
                                    onkeyup="valida_celular()">
                            </td>
                        </tr>
                        <tr>
                            <td><label class="col-form-label col-form-label-sm">Email:</label></td>
                            <td><input type="email" name='email' class="form-control form-control-sm"></td>
                        </tr>
                    </table>
                </div>
                <button type="submit" class="btn btn-sm btn-success mt-2 col-2" style='margin-left: 42%'>Enviar</button>
                <label class='col-form-label'
                    style="font-size: 12px;text-align: center;padding-left: 30%;padding-right: 30%">Será
                    encaminhado via e-mail as instruções
                    para
                    a
                    configuração inicial do sistema, e também seu usuário e sua senha inicial - que
                    poderá ser alterada a
                    qualquer
                    momento. A validade do seu acesso para o teste da plataforma será de 30 dias. Após
                    este
                    período entraremos em contato para averiguar se você gostou do sistema.</label>
            </div>
        </form>
    </div>
</div>
<script>
    function valida_celular() {
        var cel = document.getElementById('celular')
        if (cel.value.length == 1) {
            cel.value = '(' + cel.value
        }
        if (cel.value.length == 3) {
            cel.value += ')'
        }
        if (cel.value.length == 4) {
            cel.value += ' '
        }
        if (cel.value.length == 6) {
            cel.value += ' '
        }
        if (cel.value.length == 11) {
            cel.value += '-'
        }
    }


    const debounce = function (func, wait, immediate) {
        let timeout;
        return function (...args) {
            const context = this;
            const later = function () {
                timeout = null;
                if (!immediate) func.apply(context, args);
            };
            const callNow = immediate && !timeout;
            clearTimeout(timeout);
            timeout = setTimeout(later, wait);
            if (callNow) func.apply(context, args);
        };
    };

    const target = document.querySelectorAll('[data-anime]')
    var animationClass = 'animate';

    function animeScroll() {
        const winTop = window.pageYOffset + ((window.innerHeight * 3 / 4));
        target.forEach(function (element) {
            if (winTop > element.offsetTop) {
                element.classList.add(animationClass)
            } else {
                element.classList.remove(animationClass)
            }
        });
    };

    animeScroll();

    if (target.length) {
        window.addEventListener('scroll', debounce(function () {
            animeScroll();
        }, 200));
    }

</script>