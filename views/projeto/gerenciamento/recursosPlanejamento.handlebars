<style>
    .lists {
        display: flex;
        flex: 1;
        width: 100%;
    }

    .lists .list {
        display: flex;
        flex-flow: column;
        flex: 1;

        width: 100%;
        border-radius: 20px;
        min-height: 300px;

        background-color: #001263e5;
        margin: 0 15px;
        padding: 8px;
        transition: all 0.2s linear;
    }

    .lists .list .list-item {
        background-color: #F3F3F3;
        border-radius: 8px;
        text-align: center;
        margin: 4px 0px;
        cursor: pointer;
    }

    .lists .list .item-planejamento {
        background-color: #F3F3F3;
        border-radius: 8px;
        text-align: center;
        margin: 4px 0px;
        cursor: pointer;
    }

    .lists .list .item {
        background-color: #F3F3F3;
        border-radius: 8px;
        text-align: center;
        margin: 4px 0px;
        cursor: pointer;
    }
</style>

<div class="row text-center" style="width: 100%;">
    <form method='post' action='/pessoa/salvarPlanejamento'>
        <div class="row text-center mb-1 fw-bold" style="color: rgb(55,40,89);width: 100%;">
            <label class='mt-4' style="font-size: 20px;"><i class="bi bi-people"></i> Alocação de Recursos</h2><br>
        </div>
        <input type='hidden' name='id' value='{{projeto._id}}' />
        <div class="row mt-4">
            <div class="col">
                <a class='btn btn-primary' style="padding-top: 30px;padding-bottom: 30px;margin-left: 10px;"
                    href='/pessoa/recursosPlanejamento/{{projeto._id}}'>Planejamento</a>
            </div>
            {{#if projeto.temArmazenamento}}
            <div class="col">
                <a class='btn btn-secondary' style="padding: 18px"
                    href='/pessoa/recursosProjetista/{{projeto._id}}'>Projetos Elétricos</a>
            </div>
            {{else}}
            {{#if projeto.temPainel}}
            <div class="col">
                <a class='btn btn-secondary' style="padding: 18px"
                    href='/pessoa/recursosProjetista/{{projeto._id}}'>Projetos Elétricos</a>
            </div>
            {{else}}
            <div class="col">
                <a class='btn btn-secondary' style="padding-top: 30px;padding-bottom: 30px"
                    href='/pessoa/recursosProjetista/{{projeto._id}}'>Projetos Elétricos</a>
            </div>
            {{/if}}
            {{/if}}
            <div class="col">
                <a class='btn btn-secondary' style="padding-top: 30px;padding-bottom: 30px"
                    href='/pessoa/recursosAterramento/{{projeto._id}}'>Aterramento</a>
            </div>

            {{#if projeto.temArmazenamento}}
            <div class="col">
                <a class='btn btn-secondary' style="padding-top: 5px;padding-bottom: 5px"
                    href='/pessoa/recursosInversores/{{projeto._id}}'>Inversores <br> e String Box</a>
            </div>
            <div class="col">
                <a class='btn btn-secondary' href='/pessoa/recursosInstalacao/{{projeto._id}}'
                    style="padding-top: 18px;padding-bottom: 18px">Estruturas e
                    Módulos</a>
            </div>            
            {{else}}
            <div class="col">
                <a class='btn btn-secondary' style="padding-top: 18px;padding-bottom: 18px"
                    href='/pessoa/recursosInversores/{{projeto._id}}'>Inversores <br> e String Box</a>
            </div>
            <div class="col">
                <a class='btn btn-secondary' href='/pessoa/recursosInstalacao/{{projeto._id}}'
                    style="padding-top: 30px;padding-bottom: 30px">Estruturas e
                    Módulos</a>
            </div>            
            {{/if}}

            {{#if projeto.temArmazenamento}}
            <div class="col">
                <a class='btn btn-secondary' href='/pessoa/recursosEstacao/{{projeto._id}}'
                    style="padding: 18px">Estação de Armazenamento</a>
            </div>
            {{/if}}
            {{#if projeto.temPainel}}
            <div class="col">
                <a class='btn btn-secondary' href='/pessoa/recursosPainel/{{projeto._id}}' style="padding: 18px">Painél
                    Elétrico</a>
            </div>
            {{/if}}

            <div class="col-md-1">
                <a class='btn btn-secondary' href='/pessoa/recursosVistoria/{{projeto._id}}'
                    style="padding-top: 30px;padding-bottom: 30px">Vistoria</a>
            </div>

            <div class="col-md-2 mt-2">
                <div class="btn-group col-12 gap-2 mt-1">
                    {{#if cronograma.dateplaini}}
                    <button type='submit' class="btn btn-sm btn-success" onclick="getItemValue()">Salvar</button>
                    {{/if}}
                    <button href='/pessoa/limpaPlanejamento/{{projeto._id}}' {{limpaPla}}  class="btn btn-sm btn-secondary">Limpar</button>
                </div>

                <div class="btn-group col-12 gap-2 mt-1">
                    {{#if projeto.ehDireto}}
                    <a href='/projeto/direto/{{projeto._id}}' class="btn btn-primary btn-sm"> Projeto</a>
                    {{else}}
                    <a href='/gerenciamento/gerenciamento/{{projeto._id}}' class="btn btn-primary btn-sm">
                        Projeto</a>
                    {{/if}}
                    <a href='/pessoa/consulta/' class='btn btn-sm btn-warning'> <i class="bi bi-people"></i></a>                    
                    <a href='/gerenciamento/cronograma/{{projeto._id}}' class='btn btn-sm btn-primary'> <i
                            class="bi bi-arrow-return-left"></i></a>
                </div>
            </div>
        </div>
        <div class="row mt-4">
            <div class="col-md-2 text-end">
                <label class='col-form-label fw-bold'>Data de Início:</label>
            </div>
            <div class="col-md-2 mt-1">
                <input disabled type="date" name='dateplaini' class='form-control form-control-sm'
                    value='{{cronograma.dateplaini}}' />
            </div>
            <div class="col-md-1 text-end">
                <label class='col-form-label fw-bold'>Data Final:</label>
            </div>
            <div class="col-md-2 mt-1">
                <input disabled type="date" name='dateplafim' class='form-control form-control-sm'
                    value='{{cronograma.dateplafim}}' />
            </div>
            <div class="col-md-2 text-end">
                <label class='col-form-label fw-bold'>Data de Finalização: </label>
            </div>
            <div class="col-md-2 mt-1">
                <input disabled type="date" class='form-control form-control-sm' value='{{cronograma.datepla}}' />
            </div>
        </div>
        <div class="row mt-2" style="padding:20px;width: 100%;">
            <div class="row">
                <div class="lists">
                    <div class="list">
                        <label class="col-form-label col-form-label-md fw-bold text-center"
                            style="color: white">Profissionais Disponíveis</label>
                        {{#each fora}}
                        <div class="list-item" style='color: black;' draggable="true">{{pla}}<a href='/pessoa/vermais/{{id}}' style="padding-left: 5px;"><i class='bi bi-eye'></i></div>
                        {{/each}}
                    </div>
                    <div class="list">
                        <label class="col-form-label col-form-label-md fw-bold text-center"
                            style="color: white">Profissionais Alocados</label>
                        {{#each dentro}}
                        <div class="item-planejamento" style='color: black;' draggable="true">{{pla}}<a href='/pessoa/vermais/{{id}}' style="padding-left: 5px;"><i class='bi bi-eye'></i></div>
                        {{/each}}
                    </div>
                </div>
            </div>
        </div>
        <input type='hidden' class='pla0' name='pla0' id='pla0' value=''>
        <input type='hidden' class='pla1' name='pla1' id='pla1' value=''>
        <input type='hidden' class='pla2' name='pla2' id='pla2' value=''>
        <input type='hidden' class='pla3' name='pla3' id='pla3' value=''>
        <input type='hidden' class='pla4' name='pla4' id='pla4' value=''>
        <input type='hidden' class='pla5' name='pla5' id='pla5' value=''>
    </form>
</div>

<script>
    const list_items = document.querySelectorAll('.list-item');
    const lists = document.querySelectorAll('.list');
    const list_planejamento = document.querySelectorAll('.item-planejamento');

    let draggedItem = null;

    for (let i = 0; i < list_items.length; i++) {
        const item = list_items[i];

        item.addEventListener('dragstart', function () {
            draggedItem = item;
            setTimeout(function () {
                item.style.display = 'none';
            }, 0)
        });

        item.addEventListener('dragend', function () {
            this.className = 'item-planejamento';
            setTimeout(function () {
                draggedItem.style.display = 'block';
                draggedItem = null;
            }, 0);
        })

        for (let j = 0; j < lists.length; j++) {
            const list = lists[j];

            list.addEventListener('dragover', function (e) {
                e.preventDefault();
            });

            list.addEventListener('dragenter', function (e) {
                e.preventDefault();
                this.style.backgroundColor = 'rgba(40, 55, 89, 1)';
            });

            list.addEventListener('dragleave', function (e) {
                this.style.backgroundColor = 'rgba(40, 55, 86, 1)';
            });

            list.addEventListener('drop', function (e) {
                console.log('drop');
                this.append(draggedItem);
                this.style.backgroundColor = 'rgba(40, 55, 86, 1)';
            });
        }
    }

    if (list_planejamento.length > 0 || list_planejamento.length != '') {

        for (let x = 0; x < list_planejamento.length; x++) {
            const planejamento = list_planejamento[x];

            planejamento.addEventListener('dragstart', function () {
                draggedItem = planejamento;
                setTimeout(function () {
                    item.style.display = 'none';
                }, 0)
            });

            planejamento.addEventListener('dragend', function () {
                this.className = 'item';
                setTimeout(function () {
                    draggedItem.style.display = 'block';
                    draggedItem = null;
                }, 0);
            })

            for (let y = 0; y < lists.length; y++) {
                const list = lists[y];

                list.addEventListener('dragover', function (e) {
                    e.preventDefault();
                });

                list.addEventListener('dragenter', function (e) {
                    e.preventDefault();
                    this.style.backgroundColor = 'rgba(40, 55, 89, 1)';
                });

                list.addEventListener('dragleave', function (e) {
                    this.style.backgroundColor = 'rgba(40, 55, 86, 1)';
                });

                list.addEventListener('drop', function (e) {
                    console.log('drop');
                    this.append(draggedItem);
                    this.style.backgroundColor = 'rgba(40, 55, 86, 1)';
                });
            }
        }
    }

    function getItemValue() {
        const item = document.querySelectorAll('.item-planejamento');
        for (i = 0; i < item.length; i++) {
            const insitem = document.getElementsByClassName(item[i].getAttribute('class'));
            var element = insitem[i].innerText;
            switch (i) {
                case 0:
                    var insdes = document.getElementById('pla0');
                    insdes.value = element;
                    break;
                case 1:
                    var insdes = document.getElementById('pla1');
                    insdes.value = element
                    break;
                case 2:
                    var insdes = document.getElementById('pla2');
                    break;
                case 3:
                    var insdes = document.getElementById('pla3');
                    break;
                case 4:
                    var insdes = document.getElementById('pla4');
                    break;
                case 5:
                    var insdes = document.getElementById('pla5');
                    break;
            }
        }
    }
</script>